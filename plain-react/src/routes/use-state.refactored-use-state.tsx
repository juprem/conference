import { createFileRoute } from '@tanstack/react-router';
import { RefactoredUseState } from '../pages/UseState/RefactoredUseState.tsx';

export const Route = createFileRoute('/use-state/refactored-use-state')({ component: RefactoredUseState });
